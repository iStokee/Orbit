<Controls:MetroWindow x:Class="Orbit.Views.ConsoleHostWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
                      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                      xmlns:local="clr-namespace:Orbit"
                      xmlns:views="clr-namespace:Orbit.Views"
                      mc:Ignorable="d"
                      Title="Orbit Console"
                      TitleCharacterCasing="Normal"
                      Height="500"
                      Width="800"
                      MinHeight="300"
                      MinWidth="500"
                      Icon="/Orbital.jpg"
                      WindowStartupLocation="CenterScreen">

    <Controls:MetroWindow.IconTemplate>
        <DataTemplate>
            <iconPacks:PackIconMaterial Kind="Console" Height="20" Width="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </DataTemplate>
    </Controls:MetroWindow.IconTemplate>

    <Grid>
        <dragablz:TabablzControl x:Name="ConsoleTabHost"
                                 ShowDefaultCloseButton="False">
            <dragablz:TabablzControl.InterTabController>
                <dragablz:InterTabController Partition="ConsolePartition"
                                             InterTabClient="{x:Static local:ConsoleInterTabClient.Instance}" />
            </dragablz:TabablzControl.InterTabController>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="4,0">
                        <iconPacks:PackIconMaterial Kind="Console"
                                                   Width="16"
                                                   Height="16"
                                                   Margin="0,0,6,0"
                                                   VerticalAlignment="Center"/>
                        <TextBlock Text="Console" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <views:ConsoleView />
            </TabItem>
        </dragablz:TabablzControl>
    </Grid>
</Controls:MetroWindow>
