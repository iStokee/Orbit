<Controls:MetroWindow x:Class="Orbit.Views.SessionCloseDialog"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                      xmlns:viewModels="clr-namespace:Orbit.ViewModels"
                      Title="{Binding SessionName, StringFormat=Close Session: {0}}"
                      Width="560"
                      Height="560"
                      MinWidth="520"
                      MinHeight="520"
                      MaxWidth="820"
                      MaxHeight="860"
                      BorderBrush="{DynamicResource Orbit.Brushes.Action.Primary}"
                      BorderThickness="1"
                      GlowBrush="{DynamicResource Orbit.Brushes.Action.Primary}"
                      ResizeMode="CanResizeWithGrip"
                      ShowInTaskbar="False"
                      WindowStartupLocation="CenterOwner"
                      WindowTitleBrush="{DynamicResource Orbit.Brushes.Action.Primary}"
                      d:DataContext="{d:DesignInstance viewModels:SessionCloseDialogViewModel}"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      mc:Ignorable="d">

    <Controls:MetroWindow.IconTemplate>
        <DataTemplate>
            <iconPacks:PackIconMaterial
                Width="16"
                Height="16"
                Margin="4,0,0,0"
                VerticalAlignment="Center"
                Kind="Alert" />
        </DataTemplate>
    </Controls:MetroWindow.IconTemplate>

	    <Controls:MetroWindow.Resources>
	        <Style x:Key="InfoLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource Orbit.Workbench.MetaText}">
	            <Setter Property="Margin" Value="0,0,0,4" />
	        </Style>
	        <Style x:Key="InfoValueStyle" TargetType="TextBlock" BasedOn="{StaticResource Orbit.Workbench.SubsectionTitle}">
	            <Setter Property="FontSize" Value="13" />
	        </Style>
    </Controls:MetroWindow.Resources>

    <Grid Style="{StaticResource Orbit.Workbench.DialogRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource Orbit.ToolHeader}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Style="{StaticResource Orbit.ToolHeaderIcon}">
                    <iconPacks:PackIconMaterial Kind="Alert" Style="{StaticResource Orbit.ToolHeaderIconGlyph}" />
                </Border>
                <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource Orbit.ToolHeaderTitle}" Text="Close Session" />
                    <TextBlock Style="{StaticResource Orbit.ToolHeaderSubtitle}" Text="Review runtime state before terminating the embedded client." />
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource Orbit.Workbench.DialogPanel}">
                <DockPanel>
                    <Border Style="{StaticResource Orbit.Workbench.Badge.Warning}">
                        <TextBlock Style="{StaticResource Orbit.Workbench.BadgeText.Warning}" Text="Destructive Action" />
                    </Border>
                    <TextBlock
                        Margin="10,0,0,0"
                        VerticalAlignment="Center"
                        Style="{StaticResource Orbit.Workbench.HelperText}"
                        Text="This action terminates the session and closes the game client." />
                </DockPanel>
            </Border>

            <!-- Session Information Card -->
            <Border
                Grid.Row="1"
	                Style="{StaticResource Orbit.Workbench.DialogPanel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                <!-- Session Name (Spans 2 columns) -->
                <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,16">
                    <TextBlock Style="{StaticResource InfoLabelStyle}" Text="SESSION NAME" />
                    <TextBlock
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="{DynamicResource Orbit.Brushes.Action.Primary}"
                        Text="{Binding SessionName}"
                        TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <!-- Left Column -->
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,0">
                    <!-- Process ID -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Style="{StaticResource InfoLabelStyle}" Text="PROCESS ID" />
                        <TextBlock Style="{StaticResource InfoValueStyle}" Text="{Binding ProcessId}" />
                    </StackPanel>

                    <!-- Session Type -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Style="{StaticResource InfoLabelStyle}" Text="TYPE" />
                        <TextBlock Style="{StaticResource InfoValueStyle}" Text="{Binding SessionType}" />
                    </StackPanel>

                    <!-- State -->
                    <StackPanel>
                        <TextBlock Style="{StaticResource InfoLabelStyle}" Text="STATE" />
                        <TextBlock Style="{StaticResource InfoValueStyle}" Text="{Binding State}" />
                    </StackPanel>
                </StackPanel>

                <!-- Right Column -->
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,0,0">
                    <!-- Time Running -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Style="{StaticResource InfoLabelStyle}" Text="RUNNING FOR" />
                        <TextBlock Style="{StaticResource InfoValueStyle}" Text="{Binding TimeRunning}" />
                    </StackPanel>

                    <!-- Created At -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Style="{StaticResource InfoLabelStyle}" Text="STARTED AT" />
                        <TextBlock Style="{StaticResource InfoValueStyle}" Text="{Binding CreatedAt, StringFormat='{}{0:HH:mm:ss}'}" />
                    </StackPanel>

                    <!-- Injection State -->
                    <StackPanel>
                        <TextBlock Style="{StaticResource InfoLabelStyle}" Text="INJECTION" />
                        <TextBlock Style="{StaticResource InfoValueStyle}" Text="{Binding InjectionState}" />
                    </StackPanel>
                </StackPanel>
                </Grid>
            </Border>

            <!-- Warning Message -->
            <Border
                Grid.Row="2"
                Padding="16,12"
                VerticalAlignment="Top"
                Background="{DynamicResource Orbit.Brushes.Surface.Subtle}"
                BorderBrush="{DynamicResource Orbit.Brushes.Border.Subtle}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel Orientation="Horizontal">
	                    <iconPacks:PackIconMaterial
                        Width="16"
                        Height="16"
                        Margin="0,0,10,0"
                        VerticalAlignment="Top"
                        Foreground="{DynamicResource Orbit.Brushes.Action.Primary}"
                        Kind="InformationOutline" />
	                    <TextBlock
	                        Style="{StaticResource Orbit.Workbench.HelperText}"
	                        TextWrapping="Wrap">
                        <Run FontWeight="SemiBold" Text="Important:" />
                        <Run Text=" Unsaved in-game progress may be lost. Please ensure you have safely logged out before proceeding." />
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>
        </ScrollViewer>

        <Border Grid.Row="2" Style="{StaticResource Orbit.Workbench.DialogCommandBar}">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    Margin="0,0,12,0"
                    Click="CancelButton_Click"
                    IsCancel="True"
                    Style="{DynamicResource Orbit.Workbench.Button}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Width="14" Height="14" Margin="0,0,8,0" VerticalAlignment="Center" Kind="ArrowLeft" />
                        <TextBlock VerticalAlignment="Center" Text="Keep Session" />
                    </StackPanel>
                </Button>
                <Button
                    Click="CloseButton_Click"
                    IsDefault="True"
                    Style="{DynamicResource Orbit.Workbench.Button.Primary}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Width="14" Height="14" Margin="0,0,8,0" VerticalAlignment="Center" Kind="Close" />
                        <TextBlock VerticalAlignment="Center" Text="Close Session" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Controls:MetroWindow>
