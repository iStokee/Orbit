<UserControl
    x:Class="Orbit.Views.SessionsOverviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:converters="clr-namespace:Orbit.Converters"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:Orbit"
    xmlns:models="clr-namespace:Orbit.Models"
    Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}">

    <UserControl.Resources>
        <converters:SessionStateToColorConverter x:Key="SessionStateToColorConverter" />
        <converters:InjectionStateToColorConverter x:Key="InjectionStateToColorConverter" />
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <converters:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />

        <Style x:Key="SessionsRootGridStyle" TargetType="Grid">
            <Setter Property="Margin" Value="16" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity}" Value="1">
                    <Setter Property="Margin" Value="12" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity}" Value="2">
                    <Setter Property="Margin" Value="8" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SessionsHeaderBorderStyle" TargetType="Border" BasedOn="{StaticResource Orbit.ToolHeader}">
            <Setter Property="Padding" Value="12" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity}" Value="1">
                    <Setter Property="Padding" Value="8,6,8,6" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity}" Value="2">
                    <Setter Property="Padding" Value="6,4,6,4" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SessionCardStyle" TargetType="Border">
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="15" />
            <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ThemeBackground}" />
            <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Gray5}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity}" Value="1">
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="Padding" Value="12" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity}" Value="2">
                    <Setter Property="Margin" Value="0,0,0,6" />
                    <Setter Property="Padding" Value="10" />
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Accent}" />
                    <Setter Property="BorderThickness" Value="2" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Style="{StaticResource SessionsRootGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header + controls  -->
        <Border Grid.Row="0" Style="{StaticResource SessionsHeaderBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Style="{StaticResource Orbit.ToolHeaderIcon}">
                        <iconPacks:PackIconMaterial
                            Width="35"
                            Height="35"
                            Margin="10,10,0,0"
                            Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                            Kind="ViewDashboard" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock FontSize="22" FontWeight="Bold" Foreground="{DynamicResource MahApps.Brushes.IdealForeground}" Text="Session Manager" />
                        <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="{DynamicResource MahApps.Brushes.IdealForeground}" Opacity="0.85"
                                   Text="{Binding Sessions.Count, StringFormat='{}{0} Active Sessions'}" />
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource MahApps.Brushes.IdealForeground}" Text="Session Manager" />
                    <TextBlock Margin="12,0,0,0" FontSize="12" Foreground="{DynamicResource MahApps.Brushes.IdealForeground}" Opacity="0.85"
                               Text="{Binding Sessions.Count, StringFormat='{}{0} Active Sessions'}" />
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" FontSize="11" Foreground="{DynamicResource MahApps.Brushes.IdealForeground}" Text="Header:" />
                    <ToggleButton Margin="0,0,4,0" Padding="8,2" Content="Full"
                                  IsChecked="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity, Mode=TwoWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static models:SessionHeaderDensity.Full}}"
                                  Style="{StaticResource MahApps.Styles.ToggleButton}" />
                    <ToggleButton Margin="0,0,4,0" Padding="8,2" Content="Compact"
                                  IsChecked="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity, Mode=TwoWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static models:SessionHeaderDensity.Compact}}"
                                  Style="{StaticResource MahApps.Styles.ToggleButton}" />
                    <ToggleButton Padding="8,2" Content="Minimal"
                                  IsChecked="{Binding Source={x:Static local:Settings.Default}, Path=SessionsHeaderDensity, Mode=TwoWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static models:SessionHeaderDensity.Minimal}}"
                                  Style="{StaticResource MahApps.Styles.ToggleButton}" />
                </StackPanel>
            </Grid>
        </Border>

        <!--  Sessions list  -->
        <Border Grid.Row="1" Style="{StaticResource Orbit.ToolCard}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Sessions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource SessionCardStyle}">
                                <Border.InputBindings>
                                    <MouseBinding
                                        Command="{Binding DataContext.SetActiveCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        MouseAction="LeftClick" />
                                </Border.InputBindings>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <!--  Status indicator  -->
                                    <Border
                                        Grid.Column="0"
                                        Width="60"
                                        Height="60"
                                        Margin="0,0,15,0"
                                        VerticalAlignment="Center"
                                        Background="{Binding State, Converter={StaticResource SessionStateToColorConverter}}"
                                        CornerRadius="30">
                                        <Grid>
                                            <Ellipse
                                                Width="16"
                                                Height="16"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Fill="{DynamicResource MahApps.Brushes.IdealForeground}"
                                                Stroke="{DynamicResource MahApps.Brushes.Gray7}" />
                                        </Grid>
                                    </Border>

                                    <!--  Details  -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock
                                            FontSize="16"
                                            FontWeight="SemiBold"
                                            Text="{Binding Name}" />
                                        <StackPanel Margin="0,6,0,0" Orientation="Horizontal">
                                            <Border
                                                Margin="0,0,6,0"
                                                Padding="6,2"
                                                Background="{DynamicResource MahApps.Brushes.Gray8}"
                                                CornerRadius="3">
                                                <TextBlock
                                                    FontSize="11"
                                                    Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                                                    Text="{Binding State}" />
                                            </Border>
                                            <Border
                                                Margin="0,0,6,0"
                                                Padding="6,2"
                                                Background="{DynamicResource MahApps.Brushes.Accent}"
                                                CornerRadius="3">
                                                <TextBlock
                                                    FontSize="11"
                                                    FontWeight="SemiBold"
                                                    Foreground="{DynamicResource MahApps.Brushes.AccentForeground}"
                                                    Text="{Binding InjectionState}" />
                                            </Border>
                                            <TextBlock
                                                VerticalAlignment="Center"
                                                FontSize="11"
                                                Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                                                Text="{Binding CreatedAt, StringFormat='Created: {0:HH:mm:ss}'}" />
                                        </StackPanel>
                                        <Border
                                            Padding="8,4"
                                            Background="{DynamicResource MahApps.Brushes.Gray9}"
                                            BorderBrush="{DynamicResource MahApps.Brushes.Accent3}"
                                            BorderThickness="1"
                                            CornerRadius="3"
                                            Visibility="{Binding LastError, Converter={StaticResource NullToVisibilityConverter}}">
                                            <StackPanel Orientation="Horizontal">
                                                <iconPacks:PackIconMaterial
                                                    Width="14"
                                                    Height="14"
                                                    Margin="0,0,6,0"
                                                    VerticalAlignment="Center"
                                                    Foreground="{DynamicResource MahApps.Brushes.Accent}"
                                                    Kind="AlertCircle" />
                                                <TextBlock
                                                    MaxWidth="500"
                                                    FontSize="11"
                                                    Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}"
                                                    Text="{Binding LastError}"
                                                    TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>

                                    <!--  Quick actions  -->
                                    <StackPanel
                                        Grid.Column="2"
                                        VerticalAlignment="Center"
                                        Orientation="Horizontal">
                                        <Button
                                            Width="36"
                                            Height="36"
                                            Margin="0,0,6,0"
                                            Command="{Binding DataContext.FocusCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MahApps.Styles.Button.Circle}"
                                            ToolTip="Focus Client Window">
                                            <iconPacks:PackIconMaterial
                                                Width="18"
                                                Height="18"
                                                Kind="Fullscreen" />
                                        </Button>
                                        <Button
                                            Width="36"
                                            Height="36"
                                            Command="{Binding DataContext.CloseCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MahApps.Styles.Button.Circle}"
                                            ToolTip="Close Session">
                                            <iconPacks:PackIconMaterial
                                                Width="18"
                                                Height="18"
                                                Foreground="#F44336"
                                                Kind="Close" />
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
