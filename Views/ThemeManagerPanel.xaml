<UserControl
    x:Class="Orbit.Views.ThemeManagerPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker"
    xmlns:converters="clr-namespace:Orbit.Converters"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:viewModels="clr-namespace:Orbit.ViewModels"
    Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ColorPicker;component/Styles/DefaultColorPickerStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:ColorSchemeToColorConverter x:Key="ColorSchemeToColorConverter" />
            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:ColorToBrushConverter x:Key="ColorToBrushConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <Border Grid.Row="0" Style="{StaticResource Orbit.ToolHeader}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource Orbit.ToolHeaderIcon}">
                    <iconPacks:PackIconMaterial
                        Width="35"
                        Height="35"
                        Margin="10,10,0,0"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Kind="Palette" />
                </Border>

                <StackPanel
                    Grid.Column="1"
                    Margin="12,0,0,0"
                    VerticalAlignment="Center">
                    <TextBlock
                        FontSize="24"
                        FontWeight="Bold"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Text="Theme Customization" />
                    <TextBlock
                        Margin="0,4,0,0"
                        FontSize="13"
                        Foreground="{DynamicResource MahApps.Brushes.IdealForeground}"
                        Opacity="0.95"
                        Text="Transform Orbit's appearance with built-in themes or design your own custom palette" />
                </StackPanel>
            </Grid>
        </Border>

        <!--  Main Content  -->
        <Grid Grid.Row="1" Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Top Row: Live Preview + Theme Mode  -->
            <Grid Grid.Row="0" Margin="0,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>

                <Border
                    Grid.Column="0"
                    Margin="0,0,10,0"
                    Style="{StaticResource Orbit.ToolCard}">
                    <StackPanel>
                        <StackPanel Margin="0,0,0,12" Orientation="Horizontal">
                            <iconPacks:PackIconMaterial
                                Width="18"
                                Height="18"
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource MahApps.Brushes.Accent}"
                                Kind="Eye" />
                            <TextBlock
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                FontSize="15"
                                FontWeight="SemiBold"
                                Text="Live Preview" />
                        </StackPanel>

                        <StackPanel Margin="0,0,0,8" Orientation="Horizontal">
                            <Button
                                Width="96"
                                Height="30"
                                Margin="0,0,8,0"
                                Content="Primary"
                                Style="{StaticResource MahApps.Styles.Button.Square.Accent}" />
                            <Button
                                Width="96"
                                Height="30"
                                Content="Secondary" />
                        </StackPanel>

                        <Border
                            Margin="0,0,5,5"
                            Padding="10,6"
                            Background="{DynamicResource MahApps.Brushes.Accent}"
                            CornerRadius="4">
                            <TextBlock
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource MahApps.Brushes.AccentForeground}"
                                Text="Accent Background" />
                        </Border>

                        <StackPanel Orientation="Horizontal">
                            <Ellipse
                                Width="12"
                                Height="12"
                                Margin="0,0,6,0"
                                Fill="#4CAF50" />
                            <TextBlock
                                VerticalAlignment="Center"
                                FontSize="12"
                                Text="Status Indicator Example" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border
                    Grid.Column="1"
                    Margin="0"
                    Style="{StaticResource Orbit.ToolCard}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial
                                Width="18"
                                Height="18"
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource MahApps.Brushes.Accent}"
                                Kind="ThemeLightDark" />
                            <TextBlock
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                FontSize="15"
                                FontWeight="SemiBold"
                                Text="Theme Mode" />
                        </StackPanel>
                        <TextBlock
                            Margin="0,6,0,0"
                            FontSize="11"
                            Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                            Text="Choose the light or dark baseline used across Orbit." />

                        <ListBox
                            Margin="0,16,0,0"
                            Background="Transparent"
                            BorderThickness="0"
                            ItemsSource="{Binding BaseThemes}"
                            SelectedItem="{Binding SelectedBaseTheme}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,2,0">
                                        <iconPacks:PackIconMaterial
                                            Width="16"
                                            Height="16"
                                            VerticalAlignment="Center"
                                            Margin="0,0,8,0">
                                            <iconPacks:PackIconMaterial.Style>
                                                <Style TargetType="iconPacks:PackIconMaterial">
                                                    <Setter Property="Kind" Value="WeatherNight" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding}" Value="Light">
                                                            <Setter Property="Kind" Value="WeatherSunny" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </iconPacks:PackIconMaterial.Style>
                                        </iconPacks:PackIconMaterial>
                                        <TextBlock
                                            Margin="0,0,0,0"
                                            VerticalAlignment="Center"
                                            FontSize="13"
                                            Text="{Binding}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="22,14" />
                                    <Setter Property="Margin" Value="0,0,12,0" />
                                    <Setter Property="MinWidth" Value="132" />
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ThemeForeground}" />
                                    <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray8}" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border
                                                    x:Name="Container"
                                                    Background="{TemplateBinding Background}"
                                                    CornerRadius="14">
                                                    <ContentPresenter
                                                        Margin="6,2"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Container" Property="Background" Value="{DynamicResource MahApps.Brushes.Accent}" />
                                                        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.AccentForeground}" />
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Container" Property="Background" Value="{DynamicResource MahApps.Brushes.Gray7}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </StackPanel>
                </Border>
            </Grid>
            <!--  Bottom Row: Custom editor + Presets  -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource Orbit.ToolCard}" Margin="0 10 0 0">
                    <StackPanel>
                        <StackPanel Margin="0,0,0,12" Orientation="Horizontal">
                            <iconPacks:PackIconMaterial
                                Width="18"
                                Height="18"
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource MahApps.Brushes.Accent}"
                                Kind="Brush" />
                            <TextBlock
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                FontSize="15"
                                FontWeight="SemiBold"
                                Text="Custom Theme Creator" />
                        </StackPanel>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock
                                    Margin="0,0,0,4"
                                    FontSize="12"
                                    Text="Theme Name" />
                                <TextBox
                                    Height="32"
                                    FontSize="13"
                                    Text="{Binding CustomThemeName, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <TextBlock
                                Grid.Column="1"
                                Margin="16,18,0,0"
                                FontSize="11"
                                Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                                Text="{Binding SelectedBaseTheme, StringFormat='Base: {0}'}" />
                        </Grid>

                        <TextBlock
                            Margin="0,0,0,4"
                            FontSize="12"
                            Text="Accent Color" />
                        <Border
                            Padding="12"
                            Background="{DynamicResource MahApps.Brushes.Gray8}"
                            CornerRadius="14">
                            <colorpicker:StandardColorPicker
                                MinHeight="340"
                                HorizontalAlignment="Stretch"
                                PickerType="HSV"
                                SelectedColor="{Binding SelectedCustomColor, Mode=TwoWay}"
                                ShowAlpha="False"
                                Style="{StaticResource DefaultColorPickerStyle}" />
                        </Border>

                        <StackPanel Margin="0,16,0,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Text="Text Foreground" />
                                <Border
                                    Width="28"
                                    Height="28"
                                    Margin="12,0,0,0"
                                    CornerRadius="6"
                                    BorderBrush="{DynamicResource MahApps.Brushes.Gray6}"
                                    BorderThickness="1"
                                    Background="{Binding SelectedCustomForeground, Converter={StaticResource ColorToBrushConverter}}" />
                                <TextBlock
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center"
                                    FontSize="11"
                                    Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                                    Text="{Binding SelectedCustomForeground}" />
                            </StackPanel>

                            <Controls:ToggleSwitch
                                Margin="0,10,0,0"
                                Header="Custom foreground"
                                IsOn="{Binding UseCustomForeground}"
                                OffContent="Use theme default"
                                OnContent="Override with picker"
                                Style="{StaticResource MahApps.Styles.ToggleSwitch}" />

                            <Border
                                Margin="0,12,0,0"
                                Padding="12"
                                Background="{DynamicResource MahApps.Brushes.Gray8}"
                                CornerRadius="14"
                                Visibility="{Binding UseCustomForeground, Converter={StaticResource BoolToVisibilityConverter}}">
                                <colorpicker:StandardColorPicker
                                    MinHeight="260"
                                    HorizontalAlignment="Stretch"
                                    PickerType="HSV"
                                    SelectedColor="{Binding SelectedCustomForeground, Mode=TwoWay}"
                                    ShowAlpha="False"
                                    Style="{StaticResource DefaultColorPickerStyle}" />
                            </Border>
                        </StackPanel>

                        <StackPanel
                            Margin="0,16,0,0"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                            <Button
                                Width="120"
                                Height="32"
                                Margin="0,0,10,0"
                                Command="{Binding DeleteCustomThemeCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial
                                        Width="14"
                                        Height="14"
                                        Margin="0,0,6,0"
                                        Kind="Delete" />
                                    <TextBlock VerticalAlignment="Center" Text="Delete" />
                                </StackPanel>
                            </Button>
                            <Button
                                Width="120"
                                Height="32"
                                Margin="0,0,10,0"
                                Command="{Binding ApplyCustomThemeCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial
                                        Width="14"
                                        Height="14"
                                        Margin="0,0,6,0"
                                        Kind="Check" />
                                    <TextBlock VerticalAlignment="Center" Text="Apply" />
                                </StackPanel>
                            </Button>
                            <Button
                                Width="130"
                                Height="32"
                                Command="{Binding SaveCustomThemeCommand}"
                                Style="{StaticResource MahApps.Styles.Button.Square.Accent}">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial
                                        Width="14"
                                        Height="14"
                                        Margin="0,0,6,0"
                                        Kind="ContentSave" />
                                    <TextBlock VerticalAlignment="Center" Text="Save" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource Orbit.ToolCard}" Margin="10 10 0 0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                <iconPacks:PackIconMaterial
                                    Width="18"
                                    Height="18"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource MahApps.Brushes.Accent}"
                                    Kind="PaletteSwatch" />
                                <TextBlock
                                    Margin="8,0,0,0"
                                    VerticalAlignment="Center"
                                    FontSize="15"
                                    FontWeight="SemiBold"
                                    Text="Built-in Colors" />
                            </StackPanel>

                            <TextBlock
                                Grid.Row="1"
                                Margin="0,6,0,0"
                                FontSize="11"
                                Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                                Text="Preview MahApps accent palettes and apply them instantly." />

                            <ListBox
                                Grid.Row="2"
                                Margin="0,12,0,12"
                                BorderThickness="0"
                                ItemsSource="{Binding ColorSchemes}"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                SelectedItem="{Binding SelectedColorScheme}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,2" Padding="8,6">
                                            <StackPanel Orientation="Horizontal">
                                                <Border
                                                    Width="24"
                                                    Height="24"
                                                    VerticalAlignment="Center"
                                                    BorderBrush="{DynamicResource MahApps.Brushes.Gray5}"
                                                    BorderThickness="1"
                                                    CornerRadius="4">
                                                    <Border.Background>
                                                        <SolidColorBrush Color="{Binding Converter={StaticResource ColorSchemeToColorConverter}}" />
                                                    </Border.Background>
                                                </Border>
                                                <TextBlock
                                                    Margin="10,0,0,0"
                                                    VerticalAlignment="Center"
                                                    FontSize="13"
                                                    Text="{Binding}" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <Button
                                Grid.Row="3"
                                Width="150"
                                Height="34"
                                HorizontalAlignment="Left"
                                Command="{Binding ApplyThemeCommand}"
                                Style="{StaticResource MahApps.Styles.Button.Square.Accent}">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial
                                        Width="16"
                                        Height="16"
                                        Margin="0,0,6,0"
                                        Kind="Check" />
                                    <TextBlock VerticalAlignment="Center" Text="Apply preset" />
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>

                    <Border
                        Grid.Row="1"
                        Margin="10,10,0,0"
                        Style="{StaticResource Orbit.ToolCard}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                <iconPacks:PackIconMaterial
                                    Width="18"
                                    Height="18"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource MahApps.Brushes.Accent}"
                                    Kind="ContentDuplicate" />
                                <TextBlock
                                    Margin="8,0,0,0"
                                    VerticalAlignment="Center"
                                    FontSize="15"
                                    FontWeight="SemiBold"
                                    Text="Saved Custom Themes" />
                            </StackPanel>

                            <TextBlock
                                Grid.Row="1"
                                Margin="0,4,0,10"
                                FontSize="11"
                                Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                                Text="Select a saved palette to load or apply it immediately." />

                            <ListBox
                                Grid.Row="2"
                                BorderThickness="0"
                                ItemsSource="{Binding CustomThemes}"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                SelectedItem="{Binding SelectedCustomTheme}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,2" Padding="8,6">
                                            <StackPanel Orientation="Horizontal">
                                                <Border
                                                    Width="24"
                                                    Height="24"
                                                    VerticalAlignment="Center"
                                                    BorderBrush="{DynamicResource MahApps.Brushes.Gray5}"
                                                    BorderThickness="1"
                                                    CornerRadius="3">
                                                    <Border.Background>
                                                        <SolidColorBrush Color="{Binding AccentHex}" />
                                                    </Border.Background>
                                                </Border>
                                                <Border
                                                    Width="20"
                                                    Height="20"
                                                    Margin="6,0,0,0"
                                                    VerticalAlignment="Center"
                                                    BorderBrush="{DynamicResource MahApps.Brushes.Gray5}"
                                                    BorderThickness="1"
                                                    CornerRadius="3"
                                                    Background="{Binding ForegroundHex, Converter={StaticResource ColorToBrushConverter}}"
                                                    Visibility="{Binding OverrideForeground, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock
                                                        FontSize="13"
                                                        FontWeight="SemiBold"
                                                        Text="{Binding Name}" />
                                                    <TextBlock
                                                        FontSize="11"
                                                        Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                                                        Text="{Binding BaseTheme, StringFormat='Base: {0}'}" />
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
